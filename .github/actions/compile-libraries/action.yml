name: 'Compile Libraries'
description: 'Compile Libraries'

inputs:
  os:
    description: "operating system"
    required: true

runs:
  using: "composite"
  steps:
    - name: Build C++ Components (Linux/macOS)
      if: inputs.os != 'Windows'
      shell: bash
      run: |
        ./scripts/build.sh

    - name: Build C++ Components (Windows)
      if: inputs.os == 'Windows'
      shell: pwsh
      run: |
        ./scripts/build.ps1
